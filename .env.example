# LLM (local / scripts only — do not commit secrets)
LLM_PROVIDER=vertex
VERTEX_PROJECT=conifer-vision01
VERTEX_LOCATION=global
LLM_MODEL=gemini-3.5-flash

# OpenAI fallback (only when LLM_PROVIDER=openai)
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# LLM_MODEL=gpt-4o-mini
